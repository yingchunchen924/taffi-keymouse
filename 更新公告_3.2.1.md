# 塔菲键鼠 3.2.1 更新公告

发布日期：2026 年 9 月 25 日

## 本次更新

- 数据目录改为版本无关的 `%APPDATA%\\塔菲键鼠数据`。
- 以后升级到 3.3、3.4 等版本时，继续使用同一个数据目录，不再因为版本号变化而重新建立脚本库。
- 第一次启动 3.2.1 时，会自动复制旧的 `%APPDATA%\\塔菲键鼠3.0` 数据到新目录。
- 迁移识图参考图片时会同步改写为新目录路径，避免以后换电脑后继续引用旧电脑路径。
- 旧数据目录会保留，不会删除，方便回退或手动备份。
- 现有脚本、步骤、识图参考图片和文件夹状态会随数据目录一起迁移。

## 升级说明

升级前请关闭旧版塔菲键鼠。启动 3.2.1 后，软件会优先使用新的“塔菲键鼠数据”目录；确认脚本正常后，可以继续保留旧目录作为备份。

本次迁移针对 3.0/3.1 使用的 `taffi3.db` 数据目录。更早版本使用的旧数据库仍建议通过旧版导出、3.2.1 导入的方式迁移。
